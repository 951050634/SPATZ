#!/usr/bin/env python3
# Generate data for dp-cdotp (CDOTP: result = sum(x * y)). Outputs to ../data/data_${M}.h

import numpy as np
import torch
import argparse
import pathlib
import hjson

np.random.seed(42)
torch.manual_seed(42)

def array_to_cstr(a, fmt=float):
    out = "{"
    if isinstance(a, torch.Tensor):
        a = a.detach().cpu().numpy().flat
    elif isinstance(a, np.ndarray):
        a = a.flat
    for el in a:
        out += "{:.8f}, ".format(el) if fmt == float else "{}, ".format(el)
    return out[:-2] + "}"

def complex_tensor_to_cstr(t):
    if t.is_complex():
        flat = torch.view_as_real(t).flatten()
    else:
        flat = t.flatten()
    return array_to_cstr(flat, fmt=float)

def rand_complex(shape, dtype):
    return torch.complex(torch.randn(shape, dtype=dtype), torch.randn(shape, dtype=dtype))

def emit_cdotp(name, **kwargs):
    M = kwargs["M"]
    prec = kwargs["prec"]
    X, Y = kwargs["X"], kwargs["Y"]
    result = kwargs["result"]
    c_struct = "cdouble" if prec == 64 else "cfloat" if prec == 32 else "chalf"
    s = '#include "layer.h"\n\n'
    s += f"// Kernel: CDOTP (dot product), Precision: {prec}-bit\n"
    s += f"const int {name}_n = {M};\n\n"
    s += f"static {c_struct} {name}_x[{M}] __attribute__((section(\".data\"))) = " + complex_tensor_to_cstr(X) + ";\n"
    s += f"static {c_struct} {name}_y[{M}] __attribute__((section(\".data\"))) = " + complex_tensor_to_cstr(Y) + ";\n"
    s += f"static {c_struct} {name}_result = {{{result.real.item()}, {result.imag.item()}}};\n"
    return s

def main():
    parser = argparse.ArgumentParser(description="Generate data for dp-cdotp (CDOTP)")
    parser.add_argument("-c", "--cfg", type=pathlib.Path, required=True, help="Config .hjson (e.g. cdotp.hjson)")
    args = parser.parse_args()
    with args.cfg.open() as f:
        param = hjson.loads(f.read())
    assert param.get("kernel") == "CDOTP", "Config kernel must be CDOTP"
    M = param["M"]
    prec = param["prec"]
    dtype = torch.float64 if prec == 64 else torch.float32
    X = rand_complex((M,), dtype)
    Y = rand_complex((M,), dtype)
    result = torch.sum(X * Y)
    emit_str = "// Generated by gen_data.py (dp-cdotp)\n" + emit_cdotp("cdotp", M=M, prec=prec, X=X, Y=Y, result=result)
    outfile = f"data_{M}.h"
    out_path = pathlib.Path(__file__).parent.parent / "data" / outfile
    out_path.parent.mkdir(parents=True, exist_ok=True)
    with out_path.open("w") as f:
        f.write(emit_str)
    print(f"Generated {out_path}")

if __name__ == "__main__":
    main()
